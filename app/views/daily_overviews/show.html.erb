<h1 class="text-3xl font-bold mb-4">Daily Overview (<%= @date.strftime('%B %d, %Y') %>)</h1>

<%= link_to "Previous Day", daily_overview_path(date: (@date - 1).to_s, id: 0) %>
<% if @date != Date.today %>
  <%= link_to "Today", daily_overview_path(date: Date.today.to_s, id: 0) %>
<% end %>
<%= link_to "Next Day", daily_overview_path(date: (@date + 1).to_s, id: 0) %>

<div class="grid grid-cols-1 md:grid-cols-2 gap-4 px-4">
  <div>
    <h2 class="text-xl font-bold mb-2">To-do List</h2>
    <%= render "todo_list" %>
  </div>

  <div>
    <h2 class="text-xl font-bold mb-2">Habit Tracker</h2>
    <%= render "habit_tracker" %>
  </div>
</div>

<div class="flex flex-col w-full px-4">
  <h2 class="text-xl font-bold mb-2">Notes</h2>
  <%= render "notes" %>
</div>